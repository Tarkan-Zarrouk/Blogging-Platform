/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['picsum.photos', 'help.kajabi.com', 'loremflickr.com'], // idek anymore
  },
}

module.exports = nextConfig